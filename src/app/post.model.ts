export class Post {
  constructor(public headline: string, public articleURL: string, public source: string, public points: number, public user: string, public timestamp: number, public comments: string[]) {

  }
}
